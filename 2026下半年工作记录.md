# 2026年 下半年工作记录

## 2026年7月

### 2026年7月1日

**项目：** 稻城亚丁半年会, AI探索, Agent SKILL, 内部会议

**紧急重要度：** 2026-07

内部会议：
Q3目标对齐会[1h]：参加团队第三季度目标与绩效对齐会议，明确 Q3 四大方向（B/S二开质量标准与51PM系统化、Web端自闭环降本、51PM研发AI-Coding与测试流程标准化、UI/前端协同与POC AI-Coding流程），以及AI-Native转型、二开智能体量产、创造社新产品等重点工作方向，完成度100%。

稻城亚丁半年会：
AI-引擎功能[3h]：1.完成大屏 label 适配（半年会录制支持及沿途适配修改），集中在侧边栏 UI 与 WDP POI 标签两块，完成度100%。
2.左侧场景切换面板适配：将 SceneSwitchPanel 的 padding、圆角、图标尺寸、激活态发光阴影等固定 px 改为 rem，使“景点切换 / 景区漫游 / 四季切换”按钮随大屏根字号等比缩放，完成度100%。
3.左侧面板内容适配：将 SidePanelContent 的树形列表、radio、checkbox、滚动条、缩进、间距、小圆点等统一改为 rem 或字号变量，并给 el-radio 与 label 加 inline-flex + align-items: center 解决单选框与文字垂直对齐，完成度100%。
4.WDP POI label 适配：修改 entity.js，字号不再写死为 16，改为可通过 markerConfig.labelFontSize 配置并经 countSize 缩放；修正 label 背景横向偏移重复缩放导致的居中偏移问题，完成度100%。
5.全局溢出控制：在 main.scss 的 body 增加 overflow: hidden，抑制适配缩放后产生的横向/纵向外溢白边，完成度100%。

AI探索：
AI探索-Agent基础[2.5h]：1.梳理 AgentGroups（harmesAgent 本地部署工作区 + hermes-web-ui 网页管理后台）、AI-WebEngineering-demo（CircleFlow AI 全流程开发教学 Demo）、lobster-usb-builder（AI 微信助手 U 盘便携版打包工具）四个项目的定位、用途、关键目录与项目关系，输出项目总览文档并附 Mermaid 关系图，完成度100%。
2.梳理各项目本机部署路径与前置条件，明确优先部署 AI-WebEngineering-demo（依赖 Node.js/Docker/MySQL，前端 localhost:5173 快速可见效果），整理 Hermes Agent 完整部署步骤（WSL2 安装、hermes setup/doctor、Gateway、Dashboard 9119、Web UI 8648），并厘清 lobster-usb-builder 为打包工具及其封号/合规风险，完成度100%。

Agent SKILL：
AI探索-Agent-Skill[2.5h]：1.基于现有 BS 前端二开 Skill 体系（总监 / BS 厂 / WDP 厂 / 数据接入厂四层能力），探索“拖资源给智能体自动完成 AI Coding 建项目”的落地路径，明确方向为将现有 Skill 产品化成“收料→识别→调度→编码→校验”流水线，完成度100%。
2.规划总控 Agent 架构与文件骨架（bs-factory-supervisor.agent.md、intake-to-requirement.prompt.md、intake 输入模板目录与资源命名规范、requirement.json 与 .skill-state.json 数据结构），确定落地顺序 VS Code Copilot Agent → Node.js CLI → Web UI，先跑通 MVP（单页 BS 布局自动生成 + npm build 校验），完成度100%。

---

### 2026年7月2日

**项目：** AI探索, 半年会DEMO支持

**紧急重要度：** 2026-07

AI探索：
AI探索-AI应用[4h]：1.组内打通爱马仕（Hermes）后首次使用爱马仕 Studio，在空 Copilot 工作区中于 WSL 侧安装并启用 openssh-server（systemctl enable --now 确保开机自启+立即生效），写入公钥并用指纹校验、systemctl status 逐项自证 sshd 处于 active (running)。
2.在管理员 PowerShell（非 WSL）中配置 WSL→Windows 端口转发（netsh portproxy 2222→22）与入站防火墙放行规则，验证 portproxy 与防火墙规则均生效并回传局域网 IP 与连接方式。
3.学习了解 Hermes Studio 是什么：作为 Hermes Agent 的桌面应用/本地运行时/Web 控制台，可用于聊天对话、管理模型和配置文件、接入平台渠道、跑定时任务、看文件、跑编码代理等，支持桌面应用、npm CLI、Docker 三种方式运行；进一步学习了 AI 聊天（流式响应、工具调用轨迹、多会话管理、按来源分组、会话搜索）、平台渠道接入（Telegram/Discord/Slack/微信/企业微信/飞书等8个平台的统一配置）、模型与多配置文件管理（凭证池自动发现模型、Profile 克隆导入导出）、文件浏览器（本地/Docker/SSH 多后端）、Web 终端、语音输入输出（TTS/STT）、编码代理、看板、日志查看等具体功能模块，以及默认账号 admin/123456 需及时修改等使用注意事项。
4.实际使用体验：通过 Web 控制台连上本机 WSL 后，用聊天对话方式让 Agent 直接操作本地环境，验证了远程对话即可在本地文件系统创建/编辑文件、执行终端命令等操作的可行性。

其它：
其它-其它[4h]：1.为项目设计组提供半年会极致DEMO灯塔与新耀时评支持；排查灯塔（Demo_DT）项目 WDP POI 弹窗尺寸随分辨率异常放大的问题，定位到 entity.js 中 addWINDOWS 函数直接将计算结果写回入参 windowConfig 对象，导致循环内多个 POI 复用同一配置对象引用时被 countSize 分辨率比例二次放大（1920分辨率比例为1不易察觉，3840分辨率下呈现尺寸翻倍），改为局部变量接收计算结果并修复，完成度100%。
2.对新耀时评项目中修改的 FPS 显示位置相关改动进行本地拉包运行与效果验证，完成度100%。

---

### 2026年7月3日

**项目：** AI探索, 其它

**紧急重要度：** 2026-07

其它：
其它-其它[5h]：1.完成一套全栈 Web 应用（Vue3+Vite 前端 / Express+Sequelize 后端 / MySQL 兼容云数据库）的公网免费部署方案落地：前端 Vercel 静态托管、后端 Render Web Service、数据库 TiDB Cloud Starter（新加坡区），打通 HTTPS+CORS+TLS 全链路，完成度100%。
2.部署前代码改造：前端 API 地址由写死 localhost 改为 VITE_API_BASE 环境变量构建时注入；后端 CORS 支持逗号分隔多域名白名单；Sequelize 数据库连接增加 DB_SSL 开关适配云库强制 TLS；将硬编码在代码中的第三方 API Token 改为环境变量并规划重置，消除公开仓库密钥泄露风险；增加 vercel.json rewrites 解决 SPA history 路由子页面刷新 404，完成度100%。
3.排查并解决部署问题：Vercel 部署 Ready 但访问 404（monorepo 子目录项目需设置 Root Directory）、Render 误入 Blueprint 页面与环境变量空行校验失败、TiDB 控制台权限缓存 Access Denied、区分 Team Settings 与项目级 Settings、Environments 与 Environment Variables 等踩坑点，并完成注册登录、核心业务流、健康接口等部署后全链路验证，整理环境变量总表与免费额度限制（Render 15 分钟休眠冷启动、手动部署）说明，完成度100%。
4.梳理前端二开项目客户环境部署与运维流程：整理 Linux 部署方案（deploy.sh/update_config.sh 脚本上传、chmod 授权、sed 处理 CRLF、端口占用换端口、deploy_error.log 排障、update_config.sh 配置 WDP 场景包 url/order）与 Windows 部署方案（deploy.bat 管理员运行、nginx 项目增删/启停/重启、dist 包更新保留 config.json、修改 config.json 切换本地 WDP 场景、改 127.0.0.1 为 ipv4 实际地址支持局域网访问）双平台操作要点，完成度100%。

AI探索：
AI探索-AI应用[3h]：1.借助 AI Agent 完成 WSL 远程开发环境自愈方案：为支持团队成员 SSH 远程接入本机 WSL 开发环境（Hermes），部署"自愈脚本+计划任务"机制——生成 hermes-wsl-keepalive.ps1 保活脚本（检测守护进程存活→拉起 WSL→动态获取 WSL IP→幂等重建 netsh portproxy 2222→22 转发规则），注册计划任务 HermesWslKeepalive（登录触发+每 5 分钟巡检），实现 IP 漂移/重启后自动恢复，完成度100%。
2.AI 自主排障闭环验证：注册计划任务时先后遇到 UserId 参数格式错误（需"域\用户名"格式）、终端非管理员拒绝访问（改用 UAC 提权子进程）、终端多行 here-string 被截断（改单行拼接写入）等 4 处异常，均由 AI 自行诊断换路解决，并触发任务做端到端自检（WSL 状态、portproxy 规则、2222 端口 TCP 连通性），完成度100%。
3.消除计划任务 PowerShell 闪窗实现完全静默：AI 定位根因为计划任务直接启动 powershell.exe 时控制台窗口"先创建再隐藏"必然闪现，采用 wscript.exe+VBS 无窗口启动器（Wscript.Shell.Run "...", 0, False）转发启动方案，提权修改任务 Action 后验证 WSL 与端口转发正常、闪窗彻底消除；沉淀闪窗根因解法与终端 here-string 限制等可复用经验，完成度100%。

---
